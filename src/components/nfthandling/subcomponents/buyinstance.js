import React from 'react';

/**
 * This component handles the buy order on 1 or many instances.
 * Presenting a small window bellow the selection with:
 * - Buying price.
 * - Symbol price: from priceSymbol.
 * @param {Object} cbOnSucess - The item where user has clicked.
 * @param {[{Object}]} arrayInstances - send the array of selected instances.
 * @param {Object} userdata as the object holding the user logged data
 * @param {Boolean} devMode - optional to see console.logs
*/

const Buyinstance = (props) => {

    return (
        <div>
            
        </div>
    )
}
export default Buyinstance;
