import React from 'react';
import Absscreenwrapper from '../absscreenwrapper';

/**
 * Shows an important Tip/message/Notification on top of all.
 * I.e: more than 3 lines of text as a helpful tip, an important notification with long text, and so on.
 * @param {String} xclassCSS - Optional The css Extra class.
 * @param {Object} tip as the pbject as { hasLink: Boolean, link: String, title: String, message: String, hasImage: Boolean, imageUrl: String }
 * @param {Boolean} devMode - Optional to see all the props and details. default as false.
 * @param {Function} closeCb mandatory to cancel or close formulator.
 */

const Topscreentip = (props) => {
    const { xclassCSS, tip, devMode, closeCb } = props;
    todo
    return (
        <Absscreenwrapper>

        </Absscreenwrapper>
    )
}

export default Topscreentip;